This is a project for ADN Gauge.

#### install all the dependencies
`pip install -r requirements.txt`

#### run migrate to setup the database
`python manage.py migrate`

#### start the server
`python manage.py runserver`
